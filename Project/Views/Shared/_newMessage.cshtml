@model IEnumerable<Project.Models.cms_contact>

@if (Model.Count() == 0)
{
    <a href="@Url.Action("List", "Mail")"><i class="far fa-envelope"></i> @Project.Resources.MyText.newMes1</a>
}
else
{
    <a href="@Url.Action("List", "Mail")"><i class="far fa-envelope"></i>@Model.Count() @Project.Resources.MyText.newMes</a>
}

